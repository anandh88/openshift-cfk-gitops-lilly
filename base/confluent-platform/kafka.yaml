apiVersion: platform.confluent.io/v1beta1
kind: Kafka
metadata:
  name: kafka
  namespace: confluent
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 3
  image:
    application: confluentinc/cp-server:8.2.2
    init: confluentinc/confluent-init-container:3.3.0
  dataVolumeCapacity: 10Gi
  storageClass:
    name: crc-csi-hostpath-provisioner
  tls:
    secretRef: kafka-tls-secret
  listeners:
    internal:
      tls:
        enabled: true
        secretRef: kafka-tls-secret
      authentication:
        type: plain
        jaasConfig:
          secretRef: kafka-internal-sasl
    external:
      tls:
        enabled: true
        secretRef: kafka-tls-secret
      authentication:
        type: plain
        jaasConfig:
          secretRef: kafka-external-sasl
      externalAccess:
        type: route
        route:
          domain: apps-crc.testing
  metricReporter:
    enabled: true
    bootstrapEndpoint: kafka:9071
    authentication:
      type: plain
      jaasConfig:
        secretRef: kafka-internal-sasl
    tls:
      enabled: true
  dependencies:
    kRaftController:
      clusterRef:
        name: kraftcontroller
    # Fixes Control Center next-gen's "Please set up Telemetry Reporter to
    # view broker metrics" prompt. Confirmed live: controlcenter-0's own
    # embedded Prometheus runs with --web.enable-otlp-receiver and
    # scrape_configs: null - it never pulls metrics, it only accepts a push
    # from each component. This tells the broker where to push to. Separate
    # from metricReporter above, which feeds the older Kafka-topic-based
    # ConfluentMetricsReporter pipeline, not C3 next-gen's Prometheus.
    metricsClient:
      url: http://controlcenter-prometheus.confluent.svc.cluster.local:9090
  podTemplate:
    serviceAccountName: kafka-sa
    podSecurityContext:
      runAsNonRoot: true
      seccompProfile:
        type: RuntimeDefault
    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  app: kafka
    resources:
      requests:
        cpu: 500m
        memory: 2Gi
      limits:
        cpu: 2
        memory: 10Gi
  configOverrides:
    server:
      - auto.create.topics.enable=false
      - default.replication.factor=1
      - min.insync.replicas=1
      - offsets.topic.replication.factor=1
      - transaction.state.log.replication.factor=1
      - transaction.state.log.min.isr=1
      - log.retention.hours=168
